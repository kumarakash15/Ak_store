<nav class="myntra-navbar">
  <div class="nav-top container-fluid d-flex align-items-center justify-content-between">

    <!-- LEFT -->
    <div class="nav-left d-flex align-items-center">
      <a href="/seller" class="nav-logo">
        AKSTORE <span class="seller-tag">SELLER PANEL</span>
      </a>
    </div>

    <!-- RIGHT -->
    <div class="nav-right d-flex align-items-center">
      <% if (session && session.sellerId) { %>
        <a href="/seller/logout" class="seller-logout-btn">LOGOUT</a>
      <% } else { %>
        <a href="/seller/login" class="seller-login-btn">LOGIN</a>
      <% } %>
    </div>

  </div>
</nav>